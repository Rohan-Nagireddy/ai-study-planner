package com.studyplanner.controller;

// Legacy MVC controller removed — replaced by StudyTaskController (REST API).
// This file is intentionally minimal to avoid Thymeleaf / JPA compilation errors.
//
// The new REST endpoints are in:
//   com.studyplanner.controller.StudyTaskController
//   → POST   /api/tasks
//   → GET    /api/tasks
//   → GET    /api/tasks/{id}
//   → PUT    /api/tasks/{id}
//   → PATCH  /api/tasks/{id}/complete
//   → DELETE /api/tasks/{id}
