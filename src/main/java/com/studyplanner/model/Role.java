package com.studyplanner.model;

/**
 * User roles within the AI Adaptive Study Planner.
 *
 * <ul>
 *   <li>{@code USER}  – Standard student / learner account</li>
 *   <li>{@code ADMIN} – Administrative account with elevated privileges</li>
 * </ul>
 */
public enum Role {
    USER,
    ADMIN
}
